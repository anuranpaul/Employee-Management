spring.application.name:
  employee
server:
  port: 9095

spring:
  datasource:
    driver-class-name: org.mariadb.jdbc.Driver
    url: jdbc:mariadb://localhost:3306/employee_directory
    username: root
    password: secret
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
